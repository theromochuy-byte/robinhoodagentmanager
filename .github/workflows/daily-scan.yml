name: Daily Market Scan

on:
  schedule:
    # All times UTC. CT = UTC-6 (CST, Nov-Mar) / UTC-5 (CDT, Mar-Nov).
    # Crons use CST (UTC-6) so they are correct in winter; CDT they fire 1h early (fine).
    - cron: '15 14 * * 1-5'   # 8:15 AM CST / 9:15 AM CDT  — pre-open refresh + scan
    - cron: '0 19 * * 1-5'    # 1:00 PM CST / 2:00 PM CDT  — midday exit check
    - cron: '15 21 * * 1-5'   # 3:15 PM CST / 4:15 PM CDT  — post-close scan + commit
  workflow_dispatch:
    inputs:
      mode:
        description: 'Run mode'
        required: true
        default: 'morning'
        type: choice
        options: [morning, midday, evening]

jobs:
  scan:
    runs-on: ubuntu-latest
    permissions:
      contents: write   # needed to push updated data files

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          ref: claude/wonderful-cerf-sa7c0d
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Determine run mode
        id: mode
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "mode=${{ github.event.inputs.mode }}" >> $GITHUB_OUTPUT
          else
            HHMM=$(date -u +%H%M)
            if   [ "$HHMM" -lt "1500" ]; then echo "mode=morning" >> $GITHUB_OUTPUT
            elif [ "$HHMM" -lt "1900" ]; then echo "mode=midday"  >> $GITHUB_OUTPUT
            else                               echo "mode=evening" >> $GITHUB_OUTPUT
            fi
          fi

      - name: Configure git identity
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Run pipeline
        env:
          NOTIFY_EMAIL_FROM:     ${{ secrets.NOTIFY_EMAIL_FROM }}
          NOTIFY_EMAIL_PASSWORD: ${{ secrets.NOTIFY_EMAIL_PASSWORD }}
          NOTIFY_EMAIL_TO:       ${{ secrets.NOTIFY_EMAIL_TO }}
        run: |
          python3 -m swing_agent.run_daily --mode ${{ steps.mode.outputs.mode }}
